{
  "name": "chm-spotify-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: resolves full Spotify playlists for Clone Hero Chart Manager",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "wrangler": "^3.86.0"
  }
}
